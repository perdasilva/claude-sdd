Implement the current phase. Use the AskUserQuestion tool whenever you need to make a decision not covered by the spec — do not guess or assume.

Read the spec files in the most recent specs/YYYY-MM-DD-phase-\*/ directory on this branch:

1. Read plan.md and follow the task groups in order.
2. Read requirements.md for key decisions and constraints.
3. After implementation, verify all acceptance criteria in validation.md are met.
4. Run the project's full check command (e.g., `make check` or equivalent from specs/tech-stack.md) to confirm all quality gates pass.
5. Report results: what was built, how many tests pass, and any deviations from the spec.

If you encounter an ambiguity or decision not covered by the spec, use the AskUserQuestion tool to ask before proceeding.
